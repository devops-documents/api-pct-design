title: ATIS sensor
type: object
properties:
  type:
    type: string
    enum:
      - ATIS
  condition:
    type: string
    enum:
      - 'off'
      - long duration delivery
      - unresponsive
      - undefined error
  airflow_alert:
    type: boolean
required:
  - type
