title: Door sensor
type: object
description: ''
properties:
  type:
    type: string
    enum:
      - door-sensor
  condition:
    type: string
    enum:
      - initializing
      - offline
      - online
      - not installed
  door_state:
    type: string
    enum:
      - open
      - closed
      - unknown
